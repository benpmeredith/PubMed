"""
Purpose:  General configuration file.  Change to set the paths
"""

class FileConfig(object):
    # set path to whatever platform is relevant
    PATH = "C:/Work/NoSchoolViolence/PubMed"
    RAWDIR = PATH + "/data/raw/"
    EXTDIR = PATH + "/data/external/"
    INTDIR = PATH + "/data/intermin/"
    OUTDIR = PATH + "/data/processed/"